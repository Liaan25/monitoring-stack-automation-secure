# Summary of Changes - Vault Automation

## 📊 Краткое резюме изменений

Все что мы делали **вручную** на сервере теперь **автоматизировано** в скрипте `install-monitoring-stack.sh` и будет выполняться автоматически при развертывании через Jenkins.

---

## 🔧 Измененные файлы

### 1. **`install-monitoring-stack.sh`** (основной скрипт)

#### Изменения в функции `setup_vault_config()`:

**A. Извлечение секретов (строки ~1957-2050)**
- ✅ Используется `/tmp/` с случайными именами файлов для безопасности
- ✅ Устанавливаются права `600` на временные файлы
- ✅ Добавлен `trap` для автоматической очистки
- ✅ Исправлен синтаксис `jq` для корректного парсинга: `.["vault-agent"].role_id`

```bash
# БЫЛО (неправильно):
jq -r '.vault-agent.role_id' "$cred_json_path"

# СТАЛО (правильно):
jq -r '.["vault-agent"].role_id' "$cred_json_path" > "/tmp/role_id_${RANDOM}${RANDOM}.txt"
```

**B. Копирование credentials (строки ~2307-2425)**
- ✅ Создание `/opt/vault/certs/` через `sudo -n /usr/bin/mkdir -p`
- ✅ Копирование из `/tmp/` в `/opt/vault/conf/` через `sudo -n /usr/bin/cp`
- ✅ Установка владельца через `sudo -n /usr/bin/chown ${KAE}-lnx-va-start:${KAE}-lnx-va-read`
- ✅ Установка прав через `sudo -n /usr/bin/chmod 640`
- ✅ Автоматическое удаление временных файлов после копирования

```bash
# Пример кода:
sudo -n /usr/bin/cp "$TMP_ROLE_ID" /opt/vault/conf/role_id.txt
sudo -n /usr/bin/chown "${KAE}-lnx-va-start:${KAE}-lnx-va-read" /opt/vault/conf/role_id.txt
sudo -n /usr/bin/chmod 640 /opt/vault/conf/role_id.txt
```

**C. Перезапуск vault-agent (строки ~2446-2500)**
- ✅ Использует полный путь: `sudo -n /usr/bin/systemctl restart vault-agent`
- ✅ Ожидание стабилизации (5 сек)
- ✅ Проверка статуса: `sudo -n /usr/bin/systemctl is-active vault-agent`
- ✅ Ожидание генерации сертификатов (еще 5 сек)
- ✅ Проверка наличия сертификатов в `/opt/vault/certs/`

```bash
# Пример кода:
sudo -n /usr/bin/systemctl restart vault-agent
sleep 5
sudo -n /usr/bin/systemctl is-active vault-agent
sleep 5
if [[ -f "/opt/vault/certs/server_bundle.pem" && -s "/opt/vault/certs/server_bundle.pem" ]]; then
    echo "✅ Сертификаты сгенерированы"
fi
```

**D. User-юниты (строки ~4149, 5837, 5851, 5861-5883, 6125)**
- ✅ Заменено `runuser -u` на `sudo -n -u` для соответствия sudo-правилам
- ✅ Все команды `systemctl --user` используют полный путь `/usr/bin/systemctl`

```bash
# БЫЛО:
local ru_cmd="runuser -u ${mon_sys_user} --"
$ru_cmd env "$xdg_env" systemctl --user restart monitoring-prometheus.service

# СТАЛО:
local ru_cmd="sudo -n -u ${mon_sys_user}"
$ru_cmd env "$xdg_env" /usr/bin/systemctl --user restart monitoring-prometheus.service
```

---

### 2. **`sudoers.example`** (обновлен)

Добавлена секция **System-level операций** (требуют root):

```sudoers
# Копирование credentials
ALL=(root) NOEXEC: NOPASSWD: /usr/bin/cp /tmp/role_id_*.txt /opt/vault/conf/role_id.txt
ALL=(root) NOEXEC: NOPASSWD: /usr/bin/cp /tmp/secret_id_*.txt /opt/vault/conf/secret_id.txt

# Установка владельца и прав
ALL=(root) NOEXEC: NOPASSWD: /usr/bin/chown ${KAE}-lnx-va-start:${KAE}-lnx-va-read /opt/vault/conf/role_id.txt
ALL=(root) NOEXEC: NOPASSWD: /usr/bin/chmod 640 /opt/vault/conf/role_id.txt
# ... и т.д.

# Создание /opt/vault/certs/
ALL=(root) NOEXEC: NOPASSWD: /usr/bin/mkdir -p /opt/vault/certs
ALL=(root) NOEXEC: NOPASSWD: /usr/bin/chown ${KAE}-lnx-va-start:${KAE}-lnx-va-read /opt/vault/certs

# Управление vault-agent
ALL=(root) NOEXEC: NOPASSWD: /usr/bin/systemctl restart vault-agent
ALL=(root) NOEXEC: NOPASSWD: /usr/bin/systemctl status vault-agent
ALL=(root) NOEXEC: NOPASSWD: /usr/bin/systemctl is-active vault-agent
```

---

### 3. **`АВТОМАТИЗАЦИЯ-VAULT.md`** (новый файл)

Полная документация по автоматизации с:
- ✅ Описанием что делает скрипт
- ✅ Списком необходимых sudo-прав для IDM
- ✅ Инструкциями по запуску через Jenkins
- ✅ Проверкой работы
- ✅ Troubleshooting секцией

---

## 🎯 Что было достигнуто

### Безопасность (требования ИБ):
- ✅ **NOEXEC** на всех sudo-правилах - предотвращает shell escapes
- ✅ **Явные пути** - `/usr/bin/systemctl`, `/usr/bin/cp` вместо просто `systemctl`, `cp`
- ✅ **Временное хранение** - секреты в `/tmp/` с правами `600`, автоматическая очистка
- ✅ **User-space deployment** - через `systemctl --user` без system units
- ✅ **Privilege separation** - четкое разделение между `mon_ci` (CI) и `mon_sys` (runtime)
- ✅ **Minimal sudo rights** - только конкретные команды с конкретными аргументами
- ✅ **No forbidden commands without explicit paths** - `systemctl` всегда с `/usr/bin/`

### Автоматизация:
- ✅ **Извлечение секретов** - автоматически из `temp_data_cred.json`
- ✅ **Копирование в system paths** - автоматически с правильными правами
- ✅ **Создание директорий** - `/opt/vault/certs/` автоматически
- ✅ **Перезапуск vault-agent** - автоматически с проверкой статуса
- ✅ **Генерация сертификатов** - автоматически с ожиданием и проверкой
- ✅ **Управление user-юнитами** - автоматически через `sudo -u mon_sys`

### Надежность:
- ✅ **Детальное логирование** - каждый шаг выводится в stderr и лог-файл
- ✅ **Проверка статуса** - после каждой операции
- ✅ **Обработка ошибок** - fallback'и и понятные сообщения об ошибках
- ✅ **Cleanup** - автоматическое удаление временных файлов через `trap`

---

## 📝 Что нужно сделать пользователю

### 1. Создать IDM заявку
Скопируйте sudo-правила из `sudoers.example` в IDM заявку, заменив `CI84324523` на ваш KAE.

### 2. Дождаться применения прав
IDM заявка должна быть approved и deployed (~10-30 минут).

### 3. Запустить Jenkins Pipeline
Просто запустите pipeline - все остальное произойдет автоматически.

### 4. Проверить результат
```bash
# Проверка credentials
ls -lah /opt/vault/conf/role_id.txt /opt/vault/conf/secret_id.txt

# Проверка vault-agent
sudo systemctl status vault-agent

# Проверка сертификатов
ls -lah /opt/vault/certs/
```

---

## 🔄 Процесс развертывания (автоматически через Jenkins)

```
1. Jenkins стартует pipeline
   ↓
2. Скрипт извлекает секреты из temp_data_cred.json через jq
   ↓ (в /tmp/ с правами 600)
3. Скрипт копирует секреты в /opt/vault/conf/ через sudo
   ↓ (с правами 640, владелец va-start:va-read)
4. Скрипт создает /opt/vault/certs/ через sudo
   ↓ (с правами 750)
5. Скрипт создает agent.hcl с правильными путями
   ↓
6. Скрипт перезапускает vault-agent через sudo
   ↓ (ждет 5 сек)
7. Скрипт проверяет статус vault-agent
   ↓ (ждет еще 5 сек)
8. Скрипт проверяет наличие сертификатов
   ↓
9. Скрипт запускает user-юниты мониторинга
   ↓ (через sudo -u mon_sys)
10. ✅ Развертывание завершено!
```

---

## 🐛 Known Issues

### Issue 1: DNS не резолвит `secman.sigma.sbrf.ru`
**Workaround:** Добавить IP в `/etc/hosts` или обратиться к администратору сети.

### Issue 2: sudo-права еще не применились
**Workaround:** Подождать 10-30 минут после approval IDM заявки, перелогиниться.

### Issue 3: Placeholder'ы в `agent.hcl`
**Fix:** Скрипт теперь генерирует `agent.hcl` с явными путями, проблема решена.

---

## 📚 Дополнительные ресурсы

- **Полная документация:** `АВТОМАТИЗАЦИЯ-VAULT.md`
- **Требования ИБ:** `docs/security-compliance.md`
- **Sudo-правила:** `sudoers.example`
- **README:** `README-SECURE-EDITION.md`

---

**Версия:** 2.0 (Автоматизация завершена)  
**Дата:** 15.02.2026  
**Статус:** ✅ Готово к развертыванию
